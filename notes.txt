1. copula_families.py
Purpose: This is the core library for your project.
Functionality: It defines the mathematical classes for the three copula families you 
are analyzing: GaussianCopula, StudentTCopula, and ClaytonCopula.
Key Methods: It includes methods to fit the copulas to data, calculate the log_likelihood, 
and compute information criteria like aic (Akaike Information Criterion) and bic 
(Bayesian Information Criterion) which are used for model selection. It also includes 
a helper function transform_to_uniform to transform raw data into uniform marginals 
(a requirement for copula modeling).


2. distribution.ipynb
Purpose: This is likely your main analysis notebook for the Iris dataset.
Functionality: It loads the Iris dataset, calculates dependence matrices 
(using Kendall's tau), and fits the different copula families to feature pairs 
(e.g., Sepal Length vs. Sepal Width). It calculates BIC scores to determine which 
copula fits best for each pair.
Visualizations: It generates visualizations such as histograms, Q-Q plots, and 
scatter plots showing the decision boundaries (e.g., Lower Tail Dependence vs. 
Upper Tail Dependence) to visualize where each copula type performs best.


3. copula_decision_boundaries.ipynb
Purpose: This notebook focuses on developing the selection strategy using synthetic data.
Functionality: It generates synthetic data from known distributions 
(Gaussian, Clayton, Student-T), calculates statistical features (like tail dependence 
coefficients and correlation), and fits copulas to determine the "winner" via AIC.
Goal: It creates visualizations (scatter plots) to map out decision boundaries. The 
goal is to understand which statistical features (like tail dependence) best predict 
which copula family should be used, potentially allowing you to choose a copula without 
fitting all of them in the future.


4. feature_calculation_time.py
Purpose: This is a performance benchmarking script.
Functionality: It compares the computational time required to calculate simple statistical 
features (like tail dependence) versus the time required to fit all three copula models.
Goal: It helps determine if it is computationally cheaper to calculate simple features and use 
a classifier to choose a copula, versus just fitting all copulas every time. It prints timing 
statistics and a recommendation based on the speedup factor.


5. real_data.ipynb
Purpose: This appears to be a placeholder or work-in-progress notebook.
Functionality: Based on the content provided, it currently only contains a 
markdown cell saying "Include ". It is likely intended to be used for applying 
your copula selection method to other real-world datasets beyond the Iris dataset 
once your methodology is finalized.